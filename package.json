{
    "name": "windy-plugin-gsi-bvmap",
    "version": "0.2.0",
    "description": "Windy plugin that overlays Japan's GSI pale topo map at high zoom",
    "main": "dist/plugin.min.js",
    "type": "module",
    "scripts": {
        "build": "rm -rf dist && mkdir dist && SERVE=false rollup -c && cp package.json dist/",
        "start": "rollup -w -c",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "windy",
        "windy.com",
        "plugins",
        "gsi",
        "japan",
        "topo-map"
    ],
    "author": "kakugirai",
    "license": "MIT",
    "dependencies": {
        "@windycom/plugin-devtools": "^3.0.3"
    }
}
