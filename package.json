{
    "name": "windy-plugin-gsi-bvmap",
    "version": "0.1.0",
    "description": "Windy plugin that adds Japan's GSI 最適化ベクトルタイル as a map layer",
    "main": "dist/plugin.min.js",
    "type": "module",
    "scripts": {
        "build": "rm -rf dist && mkdir dist && SERVE=false rollup -c && cp package.json dist/",
        "start": "rollup -w -c",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": ["windy", "windy.com", "plugins", "gsi", "japan", "vector-tiles"],
    "author": "kakugirai@gmail.com",
    "license": "MIT",
    "dependencies": {
        "@windycom/plugin-devtools": "^3.0.3",
        "pmtiles": "^3.2.0"
    }
}
